import { ErrorMessage } from "@utils/configs/Messages/messages";

export class InfoController {
  async CreateProjectOwnerController () {
    try {

    } catch (err) { 
      ErrorMessage("InfoController", `${err}`)
    }
  }
}